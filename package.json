{
  "name": "actionpackd-site",
  "version": "1.0.0",
  "description": "Actionpackd AI SDK Website",
  "scripts": {
    "start": "npx serve",
    "build": "mkdir -p dist && cp -r index.html styles.css app.js 1.png dist/"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
